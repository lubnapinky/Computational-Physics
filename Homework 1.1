#Converting from decimal to binary representation
def binary(n):
    if n > 1:
       binary(n/2)
print ( n % 2, end = '' )  # print the reminders in the reverse order
dec=int(input("Enetr an Interger:"))
binary(dec)
