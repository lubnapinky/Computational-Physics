from math import sqrt, exp
from random import random

def f(x):
    return 1.0/(sqrt(x)*(exp(x)+1))
def w(x):
    return 1.0/(sqrt(x))

N=1000000
sum=0.0
for i in range(N):
    x=(random())**2.0
    sum+=f(x)/w(x)
print("I=",2*sum/N)
